<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:innerRadius="0dp"
    android:shape="ring"
    android:thicknessRatio="2"
    android:useLevel="false">
    <gradient
        android:endColor="@color/redTokenEdge"
        android:gradientRadius="@dimen/token_gradient_radius"
        android:startColor="@color/redToken"
        android:type="radial" />
    <stroke
        android:width="@dimen/token_outer_circle"
        android:color="@color/redTokenEdge" />
</shape>

